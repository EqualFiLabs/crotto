[profile.default]
src = "src"
test = "test"
script = "script"
out = "out"
libs = ["lib"]
solc = "0.8.33"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
bytecode_hash = "none"
via_ir = true
fs_permissions = [{ access = "read", path = "./out" }]

[fmt]
line_length = 120
tab_width = 4

[lint]
mixed_case_exceptions = ["ERC", "URI", "NFT", "POL", "WETH"]

[fuzz]
runs = 1000

[invariant]
runs = 256
depth = 50
fail_on_revert = false
